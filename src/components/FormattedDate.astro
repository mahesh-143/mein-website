---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-uk", {
      day: "2-digit",
      month: "long",
      year: "numeric",
    })
  }
</time>
